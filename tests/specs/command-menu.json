[
  {
    "name": "Russian command menu opens each destination",
    "steps": [
      {
        "send": { "text": "/profile" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "У вас пока нет профиля — создайте его за несколько минут." } }]
      },
      {
        "send": { "text": "/search" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Пока подходящих анкет нет — загляните позже." } }]
      },
      {
        "send": { "text": "/likes" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Посмотрим, кому вы понравились." } }]
      },
      {
        "send": { "text": "/matches" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Пока взаимных симпатий нет — знакомьтесь без спешки, и всё может начаться с доброго шага." } }]
      },
      {
        "send": { "text": "/messages" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Здесь хранятся ваши разговоры с взаимными симпатиями." } }]
      },
      {
        "send": { "text": "/filters" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Настройте поиск человека, с которым вам будет близко строить семью." } }]
      },
      {
        "send": { "text": "/vip" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Дополнительные возможности для серьёзных знакомств пока готовятся. Мы сообщим, когда они станут доступны." } }]
      },
      {
        "send": { "text": "/settings" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Здесь можно позаботиться о видимости анкеты и безопасности." } }]
      }
    ]
  },
  {
    "name": "A command interrupts profile creation",
    "steps": [
      {
        "send": { "callback": "profile:create:start" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Серьёзные отношения начинаются с уважения. Вы ищете партнёра для серьёзных отношений и семьи?" } }]
      },
      {
        "send": { "callback": "profile:consent:yes" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Как вас зовут?" } }]
      },
      {
        "send": { "text": "/settings" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Здесь можно позаботиться о видимости анкеты и безопасности." } }]
      },
      {
        "send": { "text": "Это не имя" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Я не совсем понял(а) сообщение. Откройте /start и выберите нужную кнопку." } }]
      }
    ]
  }
]
