[
  {
    "name": "Admin panel stays hidden from regular users",
    "steps": [
      {
        "send": { "text": "/admin" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Доступ владельца пока не настроен." }
          }
        ]
      },
      {
        "send": { "callback": "admin:open" },
        "expect": []
      }
    ]
  }
]
