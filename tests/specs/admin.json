[
  {
    "name": "Admin panel stays hidden from regular users",
    "steps": [
      {
        "send": { "text": "/admin" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Owner access isn't set up yet." }
          }
        ]
      },
      {
        "send": { "callback": "admin:open" },
        "expect": []
      }
    ]
  }
]
